# Operator-provided secret files go here (gitignored). Required:
#   postgres_password        — must match POSTGRES_PASSWORD in ../.env
# Optional (app-managed, RO-mounted at /run/himmy-secrets):
#   HIMMY_ENCRYPTION_KEY     — base64 KEK for encryption-at-rest

apiVersion: v1
kind: Service
metadata:
  name: {{ include "himmy-studio.fullname" . }}
  labels:
    {{- include "himmy-studio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "himmy-studio.selectorLabels" . | nindent 4 }}
